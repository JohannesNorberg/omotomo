% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_omotomo.R
\name{get_omotomo}
\alias{get_omotomo}
\title{Load Omotomo GNSS data from a directory using file time windows}
\usage{
get_omotomo(
  dir_path,
  t_start,
  t_stop,
  file_span_min = 15,
  lat_filt = c(-90, 90),
  long_filt = c(-180, 180),
  sat_modeling_alt = 1200000,
  tz = "UTC",
  pattern = "^omotomo\\\\d{9}_par\\\\.h5$",
  check_constant_step = TRUE,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{dir_path}{Character. Directory containing Omotomo files.}

\item{t_start}{POSIXct. Start time (UTC recommended).}

\item{t_stop}{POSIXct. Stop time (UTC recommended).}

\item{file_span_min}{Numeric. File coverage length in minutes. Default 15.}

\item{lat_filt}{Numeric vector length 2. Data latitude filter.}

\item{long_filt}{Numeric vector length 2. Data longitude filter.}

\item{sat_modeling_alt}{Numeric. Modeling altitude in meters for \verb{lat,long,alt} pierce coordinates. Default 1200e3.}

\item{tz}{Character. Timezone for filename parsing. Default "UTC".}

\item{pattern}{Character. Regex for selecting filenames. Default matches \verb{omotomo<9 digits>_par.h5}.}

\item{check_constant_step}{Logical. If TRUE, checks if end time spacing is constant.}

\item{verbose}{Logical. Print progress.}

\item{...}{Additional arguments forwarded to \code{\link{read_omotomo}}.}
}
\value{
data.table. Combined observations from selected files.
Attribute \code{files_selected} contains a data.table of selected file coverage windows.
}
\description{
Reads one or more Omotomo HDF5 files from a directory such that the returned
measurements cover the requested time interval.
}
\details{
The Omotomo filename time stamp (\code{yydoyHHMM}) is interpreted as the file end time.
Each file is assumed to cover a fixed interval of length \code{file_span_min}
ending at that time.

This wrapper:
\itemize{
\item lists Omotomo files under \code{dir_path}
\item parses end times from filenames
\item optionally checks that file end times have constant spacing
\item selects files whose coverage overlaps \verb{[t_start, t_stop]}
\item reads selected files with \code{\link{read_omotomo}}
\item filters rows to exactly \verb{[t_start, t_stop]}
}
}
