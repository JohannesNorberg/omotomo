% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_json_data.R
\name{save_json_data}
\alias{save_json_data}
\title{Save numeric reconstruction results as JSON (new tomo$ne format)}
\usage{
save_json_data(tomo, rm_negat = TRUE, status_col = NULL)
}
\arguments{
\item{tomo}{A list with at least:
\itemize{
\item tomo$ne (data.table)
\item tomo$domain$t0, tomo$domain$t1 (POSIXct or character)
\item tomo$style$latlim, tomo$style$altlim (km), tomo$style$longlim
\item tomo$utils$results_directory, tomo$utils$label_t
}}

\item{rm_negat}{Logical. If TRUE, negative posterior/background are set to zero.}

\item{status_col}{Optional quality indicator stored to JSON.}
}
\description{
Writes a JSON file containing voxel-wise electron density values on the
reconstruction grid, using the table format:
}
\details{
tomo$ne: data.table with columns:
lat, alt (m), long, background, posterior

The JSON output uses:
ne            = posterior / 1e11       (10^11 / m^3)
background_ne = background / 1e11      (10^11 / m^3)
}
